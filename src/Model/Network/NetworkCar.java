package Model.Network;

import Model.Game.Car;
import org.newdawn.slick.tiled.TiledMap;

public class NetworkCar extends Car{
    public NetworkCar(TiledMap map, int x, int y) {
        super(map, x, y);
    }
}
